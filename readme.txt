# Option Pricing App

A simple Python app to price European call and put options using the Black-Scholes model.

It has a basic GUI (built with Tkinter) where you can enter option parameters and see a live plot of the option price based on the underlying asset.

## Requirements

- Python 3
- matplotlib
- scipy
